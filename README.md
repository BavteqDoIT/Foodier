# Foodier
An application built using React Native. The application allowed scanning barcodes, then searching for products in the database and then adding food products to selected locations. The project was created as part of classes at university.
